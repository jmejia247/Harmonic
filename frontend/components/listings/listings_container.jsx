import { connect } from "react-redux";
import Listing from './listings';

const mSTP = (state) => {
  return {

  };
};

const mDTP = (dispatch) => {
  return {

  };
};

export default connect(mSTP, mDTP)(Listing);
