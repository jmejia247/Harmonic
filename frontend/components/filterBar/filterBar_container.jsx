import React from "react";
import { openModal } from "../../actions/modal_actions";
import { connect } from "react-redux";
import FilterBar from "./filterBar";

const mSTP = (state) => {
  return {
      
  };
};

const mDTP = (dispatch) => {
  return {

  };
};

export default connect(mSTP, mDTP)(FilterBar);
